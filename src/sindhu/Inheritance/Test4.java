/*we are attempting to create an object of the parent class (A) and 
 * assign it to a reference variable of the child class (B). 
 * This results in a compilation error (CE), 
 * because we cannot assign an object of the parent class to a reference 
 * variable of the child class.*/

package sindhu.Inheritance;

public class Test4 {
	public static void main(String[] args) {
		/*Employee emp = new Manager();
		
		System.out.println(emp.id);
		System.out.println(emp.name);
		System.out.println(emp.departmentId);
		System.out.println(emp.departmentName);
		System.out.println(' ');
		
		emp.getId();
		emp.getName();
		emp.getDepartmentName("IT");
		emp.getEmployeeDeptId();
		emp.getDepartmentName();*/
	}
	
}
